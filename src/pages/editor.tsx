import React from 'react';import { EditorProvider } from '@/context/EditorContext';import { DndProvider } from 'react-dnd';import { HTML5Backend } from 'react-dnd-html5-backend';import { EditorLayout } from '@/components/editor/EditorLayout';import { Toaster } from '@/components/ui/toaster';/** * 🏆 PÁGINA PRINCIPAL DO EDITOR UNIFICADA *  * Esta é a entrada principal para o editor Quiz Quest. * Combina todas as funcionalidades dos editores anteriores em uma estrutura limpa. *  * Funcionalidades: * ✅ Layout de 4 colunas responsivo * ✅ 21 etapas do funil * ✅ Sistema de propriedades avançado (10+ editores) * ✅ Canvas com drag & drop * ✅ Preview responsivo * ✅ Auto-save * ✅ Navegação por etapas */export default function EditorPage() {  return (    <div className="h-screen w-full overflow-hidden">      <EditorProvider funnelId="main-funnel">        <DndProvider backend={HTML5Backend}>          <EditorLayout />          <Toaster />        </DndProvider>      </EditorProvider>    </div>  );}