/**
 * 🏗️ BUILD-TIME TEMPLATES EMBEDDED
 * 
 * Este arquivo é gerado automaticamente pelo script build-templates
 * Contém todos os templates em formato Block[] para L3 cache
 * 
 * Executar: npm run build:templates
 */

// Placeholder: será substituído por templates reais no build
export default {} as Record<string, any[]>;
