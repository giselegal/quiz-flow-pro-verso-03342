/**
 * TEMPORARY SIMPLIFIED TEMPLATE FUNCTION TO FIX QUIZ ISSUE
 */

export function getPersonalizedStepTemplate(stepId: string, funnelId?: string): any {
    // Return null for now - this will make the hook fallback to default QUIZ_STEPS
    return null;
}

export function getStepTemplate(stepId: string): any {
    return null;
}