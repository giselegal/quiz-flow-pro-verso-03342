/**
 * ConsolidatedProvider Stub
 */
import React, { ReactNode } from 'react';

export function ConsolidatedProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

export default ConsolidatedProvider;
