/**
 * EditorAccessControl Stub
 */
import React, { ReactNode } from 'react';

interface EditorAccessControlProps {
  children: ReactNode;
}

export function EditorAccessControl({ children }: EditorAccessControlProps) {
  return <>{children}</>;
}
