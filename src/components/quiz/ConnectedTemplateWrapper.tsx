/**
 * ConnectedTemplateWrapper Stub
 */
import React, { ReactNode } from 'react';

export function ConnectedTemplateWrapper({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

export default ConnectedTemplateWrapper;
