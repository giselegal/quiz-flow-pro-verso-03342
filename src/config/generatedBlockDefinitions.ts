import { BlockDefinition } from './blockDefinitions';

// Definições geradas automaticamente de todos os componentes *Block.tsx
// Atualizado pelo script em scripts/generate-block-definitions.ts

export const generatedBlockDefinitions: BlockDefinition[] = [];
