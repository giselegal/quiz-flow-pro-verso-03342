/**
 * 📦 EDITOR HOOKS - Barrel Export
 * 
 * Exportações consolidadas dos hooks de editor.
 */

export { useEditorAdapter, useEditorAdapter as default } from './useEditorAdapter';
export { 
  useEditorUnified, 
  useEditorUnifiedOptional,
  isEditorCompatAPI,
  type UseEditorUnifiedOptions 
} from './useEditorUnified';
